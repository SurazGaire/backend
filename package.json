{
  "name": "backend-phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../the-phonebook/ && npm run build && cp -r build ../backend-phonebook",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SurazGaire/backend-phonebook.git"
  },
  "author": "Suraj Gaire",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SurazGaire/backend/issues"
  },
  "homepage": "https://github.com/SurazGaire/backend#readme",
  "dependencies": {
    "express": "^4.17.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
